name: Docker image build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Login to DockerHub
        run: |
          echo ${{ secrets.DOCKERTOKEN }} | docker login -u "apratim14" --password-stdin

      - name: Build Docker Image
        run: |
          docker build -t apratim14/fastapi-app:v0.1 ./fastapiserver+ga

      - name: Push Docker Image
        run: |
          docker push apratim14/fastapi-app:v0.1
